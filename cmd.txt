
echo -n 'username' | base64
echo -n 'password' | base64


By default, if no specific environment variables are set, the mongo-express web interface uses:
Username: admin
Password: pass

chmod +x deploy.sh
./deploy.sh

chmod +x cleanup.sh
./cleanup.sh


make            # runs: start, apply, access
make cleanup    # removes all resources and stops minikube
make access     # reopens Mongo Express service in browser
